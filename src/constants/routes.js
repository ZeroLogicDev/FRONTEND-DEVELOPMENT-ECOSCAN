/**
 * Route path constants.
 * Single source of truth for all navigation paths.
 */
export const ROUTES = {
  LANDING: '/',
  LOGIN: '/login',
  AUTH_CALLBACK: '/auth/callback',
  DASHBOARD: '/dashboard',
  SCAN: '/scan',
  HISTORY: '/history',
  PROFILE: '/profile',
  NOT_FOUND: '/404',
};
